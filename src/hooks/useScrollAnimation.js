import {useSpring} from "react-spring"

/**
 * @param animationOptions
 */
export default function useScrollAnimation() {


}

// Create intersection observer with a border that triggers before element is visible
// Get the element position and mount a scroll callback with element dims
// tween between the desired keyframes
//

{props, ref} = useScrollAnimation({from: {transform: "translateX(0)"}, to: {transform: "translateX(70px)"}})
